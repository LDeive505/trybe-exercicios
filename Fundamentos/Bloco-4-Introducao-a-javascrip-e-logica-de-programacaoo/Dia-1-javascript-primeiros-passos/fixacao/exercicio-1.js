const base = 5;
let height = 8;
const area =  base * height;
const perimeter = 3 + 4 + 3; // valor dos lados de um triângulo qualquer

console.log("area: " + area);
console.log("Perímetro: " + perimeter);